spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/messenger
    username: messenger
    password: messenger

server:
  port: 8081
  servlet:
    session:
      cookie:
        max-age: 30m
        http-only: true
        secure: false

logging:
  level:
    ru.yofik.athena.auth: debug
    org.springframework.security: trace