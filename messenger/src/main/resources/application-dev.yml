server:
  port: 8080
#  ssl:
#    enabled: false
#    protocol: TLS
#    enabled-protocols: TLSv1.2
#    key-store: certs/Server-keystore.p12
#    key-store-type: PKCS12
#    key-store-password: password
#    client-auth: need
#    trust-store: certs/Server-truststore.p12
#    trust-store-type: PKCS12
#    trust-store-password: password

spring:
  datasource:
    url: jdbc:postgresql://172.25.0.2:5432/messenger
    username: messenger
    password: messenger
  jpa:
    hibernate:
      ddl-auto: validate
  redis:
    host: 172.25.0.5
    port: 6379
    ssl: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8081/api/v2/auth/jwks
          jws-algorithms: ES256

auth-service:
  baseUrl: http://localhost:8081/
  truststore: <configure me for stage and prod>
  truststorePassword: <configure me for stage and prod>